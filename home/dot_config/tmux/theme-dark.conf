source-file ~/.config/tmux/tmuxline-dark.conf

# Use pane borders, not pane background tinting, so OSC 11 background queries
# can still reflect the attached client terminal.
set -g pane-border-style fg=colour235,bg=colour238
set -g pane-active-border-style fg=colour236,bg=colour250
