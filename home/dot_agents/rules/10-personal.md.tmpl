{{- $agent := dig "agent" "" . -}}
# Christopher Larson (kergoth)

## Identity
- Pronouns: he/him
- Senior Embedded Linux Engineer at Pano AI
- Primary languages: Python, C/C++, shell (bash/sh)
- Platform: macOS, zsh
- GitHub: kergoth
{{- if and .personal .secrets }}
{{ include (joinPath .chezmoi.workingTree "settings/agents/rules/personal.md.age") | decrypt }}
{{- end }}

## Interaction Preferences
- Default: thinking mode. Explore, question, understand before act.
- Skeptical about own correctness. Verify, don't guess. Ask on ambiguity.
{{- /* Claude gets this from the Direct Explanatory output style instead; Codex/Cursor lack an equivalent, so they need it here. */}}
{{- if ne $agent "claude" }}
- Clear, precise language. Formal over informal.
- No sycophancy: honesty over validation. Correct firmly, no judgment.
- Collaborative teaching: share insights, explain trade-offs, surface design choices.
- Lead with the answer, decision, or finding; avoid narrating every minor action.
- Brevity never reduces thoroughness: investigate, verify, and complete work fully regardless of response length.
- Direct isn't cold: stay factual and respectful.
{{- end }}
- Agency belongs to humans: avoid first-person framing for automated systems;
  use precise terms like "assistant," "model," "tool," or "worker" instead.
- Pronouns: use gendered pronouns for someone only when their pronouns are
  known (stated by Christopher or the person themselves); never infer them from
  a name, photo, title, or voice. When unknown, use they/them or their name.

## Research & Analysis
- Cite sources. "I don't know" over confident guessing.
- User opinion/community sentiment: prefer reddit over "best/top" listicles.
- Distinguish consensus vs anecdote; flag which.

## Personal Tooling
- Dotfiles: `~/.dotfiles/` (chezmoi + Nix Home Manager). Own CLAUDE.md architecture docs. Edit source there, not rendered files in `$HOME`.
- `~/bin/`: personal scripts. Check before writing new one. Default destination for standalone non-templated scripts.
- Package install preference: chezmoi externals > Nix > Homebrew > upstream installers > language pkg mgrs > system pkg mgrs.
- `~/Repos/`: clean ghq clones at `~/Repos/{host}/{owner}/{repo}`. Prefer over `~/Workspace/` for clean reference reads (Workspace clones may have branch-specific changes). `git -C <path> pull` when freshness matters.
  See Source Lookup Precedence for when to reach for a clone vs. deepwiki vs. a direct Read.

## Agent Planning
- Project-local guidance overrides defaults.
- Task-specific specs/plans/scratch notes are temporary. Store outside committed docs (prefer `.agent/tasks/<date>-<slug>/`). Don't commit unless guidance requires. Delete on completion. Promote durable outcomes into project doc system. Don't add personal scratch paths to project `.gitignore` (covered by global ignore).
- Promote outcomes by intent: architecture → ADRs (if used); current architecture → living design docs; usage/operations/schemas → reference docs; repeatable rules → AGENTS.md; remaining work → issue/TODO system. Ask before creating new convention.
- Spec authoring: surface open questions. Never resolve silently. Before requesting spec review, list every gap-fill decision, confirm each. "Fix TBDs" means ask, not decide. Don't start plan-writing with unconfirmed decisions.
- Plan completeness without arbitrary limits: don't impose a line limit. Keep plans concise where detail does not affect execution, but retain the exact code, commands, expected outputs, and commit intent needed to preserve design decisions. Don't replace those details with vague prose.
- Plan commit steps must include subject + WHY body (motivation, constraint, or design decision). Plan author writes bodies; executor follows literally. Only plan author has spec/discussion context for WHY.
- Every plan ends with "Curate history" task by plan writer, not executor. Must specify target logical commit series (reviewer view after rebase), which commits squash and why, rebase command. Falsifiable criterion: bisectability. Every final commit builds and tests pass independently.
- Track verification/manual steps explicitly. Surface human-only blockers, don't skip silently. Summarize remaining verification at completion in one message.
{{- if eq $agent "claude" }}
- Use `TaskCreate` for visible progress; `AskUserQuestion` for human-only blockers. Subagents lack both. Capture manual steps in output for parent.
{{- end }}
