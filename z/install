Z_DATA_DIR=$XDG_DATA_HOME/z
mkdir -p $Z_DATA_DIR

if [ -e ~/.z ] && [ ! -e $Z_DATA_DIR ]; then
    # Copy existing data to new location
    cp ~/.z $Z_DATA_DIR/data
else
    touch $Z_DATA_DIR/data
fi
