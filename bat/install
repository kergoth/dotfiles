case "$OSTYPE" in
    darwin*)
        cfgpath=~/Library/Preferences
        ;;
    *)
        cfgpath=$XDG_CONFIG_HOME
        ;;
esac
link . $cfgpath/bat
ln -sf base16-tomorrow-night.tmTheme themes/Default.tmTheme
