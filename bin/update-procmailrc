#!/bin/sh

#cp ~/.procmailrc ~/.procmailrc.bak
#cat $HOME/.procmail.d/* > ~/.procmailrc

for i in $HOME/.procmail.d/*; do
    if [ -f "$i" ]; then
        echo "## $i BEGIN"
        cat "$i"
        echo "## $i END"
        echo ""
    fi
done > ~/.procmailrc
