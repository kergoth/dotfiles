#!/bin/sh

toremove=""
for item in "$@"; do
    dir=$(dirname $item)
    tmpdir=$(mktemp -d $dir/bgrm.XXXXXX) || exit 1
    mkdir -p $tmpdir
    toremove="$toremove $tmpdir"
    mv "$item" "$tmpdir/"
done
rm -rf $toremove &
