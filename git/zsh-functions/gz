if [[ $# -eq 0 ]]; then
    local query=
else
    local query="'$*"
fi
local repo="$(gg | fzf-tmux --exit-0 ${query:+--select-1 -q "$query"})" || return $?
if [[ -z "$repo" ]]; then
    return 1
fi
gg "$repo"
