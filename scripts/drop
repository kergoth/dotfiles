#!/bin/sh

case "$(uname -s)" in
    Darwin)
        alias remove=trash
        ;;
    *)
        alias remove=bgrm
        ;;
esac

for arg; do
    if [ -h "$arg" ]; then
        rm -fv "$arg"
    else
        remove "$arg"
    fi
done
