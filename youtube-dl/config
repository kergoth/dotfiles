# Grab and embed subtitles if available
--write-sub
--write-auto-sub
--embed-subs

# Metadata
--add-metadata
--xattrs

# Prefer the best combined format by default, rather than bestvideo+bestaudio
-f best
