*) implement hash for lum and sat as well for url scheme.
*) save the user selected color.
*) implement tagged browsing.
	---expected functionality for tagged browsing--
	user sets a tag on a tab, the tab's color is calculated by the hash of the tagname
	user sets a tag on a tab, the tab's color is userprovided

*) a list of 10 tags must be customisable with the color of choice.
*) a good colorpicker.

