#compdef tmx

_tmx() {
  local -a groups
  tmux_groups=($(tmux ls -F '#{?session_grouped,#{session_group},}' 2>/dev/null | sort -u))
  _arguments '*:groups:($tmux_groups)'

  return 0
}

_tmx

# vim: ft=zsh sw=2 ts=2 et
