# Named directory bits
if [[ -e ~/code ]]; then
    namedir code ~/code
elif [[ -e ~/Code ]]; then
    namedir code ~/Code
fi
for i in ~code/*(N/); do
    namedir ${${i:t}//[-_]/} $i
done

case $HOST in
    (*)
        ;;
esac
